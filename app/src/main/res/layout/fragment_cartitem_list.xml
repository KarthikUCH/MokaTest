<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="moka.pos.test.ui.shoppingcart.ShoppingCartFragment">

    <android.support.v7.widget.RecyclerView
        android:id="@+id/recycleview_items"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/btn_clear"></android.support.v7.widget.RecyclerView>


    <Button
        android:id="@+id/btn_clear"
        style="@style/TextAppearance.AppCompat.Headline"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/tv_total"
        android:background="@android:color/holo_red_light"
        android:text="@string/text_btn_clear"
        android:textColor="@android:color/white" />

    <TextView
        android:id="@+id/tv_total"
        style="@style/TextAppearance.AppCompat.Headline"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:gravity="center"
        android:text="@string/text_lbl_price"
        android:textSize="36dp" />

</RelativeLayout>
